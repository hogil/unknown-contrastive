# Archived legacy data manifests

The nine JSON files in this directory were moved from `D:\project\unknown-contrastive\data\images` on 2026-07-26.

Their derived image trees had already been removed during single-canonical-dataset cleanup, and repository source contained no direct filename reference. They are retained only as provenance. Active canonical pool manifests remain in:

- `D:\project\unknown-contrastive\data\pools`

The only canonical image dataset tree is:

- `D:\project\unknown-contrastive\data\images\unknown_train_all`
