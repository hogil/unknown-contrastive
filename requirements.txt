# ============================================================
#  Python: 3.10+ 필요 (개발은 Python 3.13.7 으로 진행)
#  scripts/ 의 type hint (str | Path, dict[K, V]) 가 3.10+ 문법
# ============================================================
#
#  ① PyTorch (별도 install — CUDA 버전 맞춤):
#     GPU CUDA 12.4:
#         pip install torch==2.6.0 torchvision==0.21.0 \
#                     --index-url https://download.pytorch.org/whl/cu124
#     CPU only:
#         pip install torch==2.6.0 torchvision==0.21.0
#
#  ② 나머지 dependency (이 파일):
#         pip install -r requirements.txt
# ============================================================

# --- Core deep learning (PyTorch 는 위 ① 으로 별도 install) ---
torch>=2.2,<3.0              # tested: 2.6.0+cu124
torchvision>=0.17            # tested: 0.21.0+cu124

# --- Model architecture (timm + ConvNeXtV2) ---
timm>=1.0                    # tested: 1.0.27 — ConvNeXtV2 FCMAE pretrained backbone
Pillow>=10.0                 # tested: 11.3.0

# --- Clustering + metrics ---
hdbscan>=0.8.40              # tested: 0.8.42 — wafer cluster
scikit-learn>=1.3            # tested: 1.7.0  — AMI/ARI/F1/Completeness/Homogeneity
scipy>=1.10                  # tested: 1.16.0 — hdbscan dependency

# --- Data / serialization ---
numpy>=1.24                  # tested: 2.2.6 (NumPy 2.x OK with torch 2.6+)
pandas>=2.0                  # tested: 2.3.1
pyarrow>=14.0                # tested: 21.0.0 — predict_grouping_prod.py parquet 출력
PyYAML>=6.0                  # tested: 6.0.1  — config / split YAML

# --- System (resource monitoring, 선택) ---
psutil>=5.9                  # tested: 5.9.5
