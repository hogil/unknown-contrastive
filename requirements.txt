# ============================================================
#  H100 / H200 (Hopper, sm_90) 둘 다 호환
#  Python 3.10+ 필요 (개발은 Python 3.13.7)
# ============================================================
#
#  ① PyTorch (별도 install — H100/H200 호환 CUDA 12.4+):
#
#     [권장 / tested] torch 2.6.0 + CUDA 12.4:
#         pip install torch==2.6.0 torchvision==0.21.0 \
#                     --index-url https://download.pytorch.org/whl/cu124
#
#     [최신] torch 2.7.0 + CUDA 12.6:
#         pip install torch==2.7.0 torchvision==0.22.0 \
#                     --index-url https://download.pytorch.org/whl/cu126
#
#     [CPU only — 학습 X, 코드 확인용]:
#         pip install torch==2.6.0 torchvision==0.21.0
#
#  ② 나머지 dependency (이 파일):
#         pip install -r requirements.txt
#
#  ★ torch ↔ torchvision 짝 (절대 misversion 안 됨):
#       torch 2.4 ↔ torchvision 0.19
#       torch 2.5 ↔ torchvision 0.20
#       torch 2.6 ↔ torchvision 0.21  ← tested
#       torch 2.7 ↔ torchvision 0.22
# ============================================================

# --- Core deep learning ---
# H100/H200 호환 range (cu124 또는 cu126 wheel — 위 ①으로 install)
torch>=2.4,<2.8                # tested: 2.6.0+cu124
torchvision>=0.19,<0.23        # tested: 0.21.0+cu124  (torch 짝 자동)

# --- Model architecture ---
timm>=1.0                      # tested: 1.0.27 — ConvNeXtV2 FCMAE
Pillow>=10.0                   # tested: 11.3.0

# --- Clustering + metrics (NumPy 2.x 호환 버전) ---
hdbscan>=0.8.40                # tested: 0.8.42
scikit-learn>=1.5              # tested: 1.7.0  (NumPy 2.x 호환)
scipy>=1.13                    # tested: 1.16.0 (NumPy 2.x 호환)

# --- Data / serialization ---
numpy>=1.24                    # tested: 2.2.6  (torch 2.4+ NumPy 2.x OK)
pandas>=2.0                    # tested: 2.3.1
pyarrow>=14.0                  # tested: 21.0.0 — predict_grouping_prod.py parquet
PyYAML>=6.0                    # tested: 6.0.1

# --- System ---
psutil>=5.9                    # tested: 5.9.5
