# sota_h100 deps — Python 3.11.
# NOTE: install torch + torchvision SEPARATELY with the CUDA wheel first (see ENV.md):
#   pip install torch==2.4.1 torchvision==0.19.1 --index-url https://download.pytorch.org/whl/cu124
# (H100 / H200 are both sm_90 -> the cu124 build covers both.)
timm>=1.0.3
numpy>=1.26,<2.2
pandas>=2.0
pillow>=10.0
scikit-learn>=1.3
pyarrow>=14.0
